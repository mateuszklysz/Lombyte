# `dumps/` — local disc images (ignored by Git)

Place your own, legally obtained disc image here. `make iso` picks the first
`dumps/*.iso` and writes the rebuilt image under `build/`.

Nothing in this directory is committed, and the expected input is documented
in the top-level README's **Building** section.
