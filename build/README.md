# `build/` — generated output (ignored by Git)

Build output lands here, for example `Ratchet & Clank (USA) - rebuilt.iso`,
and the baseline workspace (`build/baseline`) created by
`./verify-baseline.sh`. The directory is recreated by the build; nothing here
is committed.
