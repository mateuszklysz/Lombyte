/*
STATE: INTENTIONAL_LOW_LEVEL_ASM
SYMBOL: _start
SCORE: code=100 functions=100 data=100 complete_data=100
DECISION: retained (intentional asm)
*/

#include "asm.h"

/* ROLE: recovered whole function `_start`. */

/* Exact SDK/library unit entry; symbolic expected assembly retained pending source recovery. */

INCLUDE_ASM("config/us/expected/asm/assembly/sdk/library/entry/entry.s", entry);
