/*
STATE: INTENTIONAL_LOW_LEVEL_ASM
SYMBOL: exponent
SCORE: code=100 functions=100 data=100 complete_data=100
DECISION: retained (intentional asm)
*/

#include "asm.h"

/* Exact SDK/library unit exponent; symbolic expected assembly retained pending source recovery. */

INCLUDE_ASM("config/us/expected/asm/assembly/sdk/library/exponent/exponent.s", exponent);
