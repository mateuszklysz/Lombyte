#include "types.h"
#include "asm.h"

#ifndef NON_MATCHING
INCLUDE_ASM("config/us/expected/asm/assembly/textbin/rendering/create_point_light/FUN_00201ba8.s", FUN_00201ba8);
#else
/* No C body yet: the previous body was a contaminated export (a
   BlackJack casino translation unit) and has been removed rather
   than replaced by a guess. */
#endif /* NON_MATCHING */
