#include "types.h"
#include "asm.h"

#ifndef NON_MATCHING
INCLUDE_ASM("config/us/expected/asm/assembly/textbin/fun_001f2260/FUN_001f2260.s", FUN_001f2260);
#else
/* No C body yet: the previous body was a contaminated export (a
   BlackJack casino translation unit) and has been removed rather
   than replaced by a guess. */
#endif /* NON_MATCHING */
