diff --git a/gcc/calls.c b/gcc/calls.c
index c4e29c7..a183545 100644
--- a/gcc/calls.c
+++ b/gcc/calls.c
@@ -24,6 +24,8 @@ Boston, MA 02111-1307, USA.  */
 #include "tree.h"
 #include "flags.h"
 #include "expr.h"
+/* ASTRA P15: opt-in suppression of Cygnus sibling-call expansion.  */
+extern char *mips_astra_no_sibcall;
 #include "regs.h"
 #include "insn-flags.h"
 #include "toplev.h"
@@ -2028,6 +2030,7 @@ expand_call (exp, target, ignore)
 	      || 1
 #endif
 	      || flag_exceptions
+	      || mips_astra_no_sibcall
 	      || ! stmt_loop_nest_empty ()
 		 /* The structure value address is used and modified in the
 		    loop below.  It does not seem worth th effort to save and
diff --git a/gcc/config/mips/mips.c b/gcc/config/mips/mips.c
index eaaa62e..d57afd5 100644
--- a/gcc/config/mips/mips.c
+++ b/gcc/config/mips/mips.c
@@ -206,6 +206,8 @@ int mips_abi;
 #endif
 
 /* Strings to hold which cpu and instruction set architecture to use.  */
+/* ASTRA P15: opt-in suppression of sibling-call expansion.  */
+char *mips_astra_no_sibcall;
 char *mips_cpu_string;		/* for -mcpu=<xxx> */
 char *mips_isa_string;		/* for -mips{1,2,3,4} */
 char *mips_abi_string;		/* for -mabi={32,n32,64,eabi} */
diff --git a/gcc/config/mips/mips.h b/gcc/config/mips/mips.h
index 9cb0fce..9770ec4 100644
--- a/gcc/config/mips/mips.h
+++ b/gcc/config/mips/mips.h
@@ -593,11 +593,15 @@ extern void		mips_select_section ();
    will be set to the string `"512"'.
 
 	extern char *m88k_short_data;
-	#define TARGET_OPTIONS { { "short-data-", &m88k_short_data } }  */
+
+#define TARGET_OPTIONS { { "short-data-", &m88k_short_data } }  */
+
+extern char *mips_astra_no_sibcall;
 
 #define TARGET_OPTIONS							\
 {									\
   SUBTARGET_TARGET_OPTIONS						\
+  { "astra-no-sibcall", &mips_astra_no_sibcall },\
   { "cpu=",	&mips_cpu_string	},				\
   { "ips",	&mips_isa_string	},				\
   { "entry",	&mips_entry_string	},				\
