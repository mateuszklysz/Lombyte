diff --git a/gcc/config/mips/mips.c b/gcc/config/mips/mips.c
--- a/gcc/config/mips/mips.c
+++ b/gcc/config/mips/mips.c
@@ -8782,7 +8782,7 @@
 		    for (d = stealable ? distance + 1 : distance; d <= 6; d++)
 		      pads++;
 
-		    if (stealable && pads == 1)
+		    if (stealable)  /* ASTRA P41: anchor all pads */
 		      anchor = prev;
 
 		    while (pads-- > 0)
